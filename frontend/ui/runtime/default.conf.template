server {
    listen 80;
    server_name  proxy;

    location /health {
        add_header Content-Type text/plain;
        return 200 "OK";
    }

    location / {
        proxy_pass ${BACKEND_ENDPOINT};
        proxy_http_version 1.1;
    }
}
